INCLUDE_DIRECTORIES( BEFORE "${CELLSEGMENTATION_SOURCE_DIR}/3DCellSegmentation/CellForegroundExtraction"
  "${CELLSEGMENTATION_BINARY_DIR}/3DCellSegmentation/CellForegroundExtraction" )
INCLUDE_DIRECTORIES( BEFORE "${CELLSEGMENTATION_SOURCE_DIR}/3DCellSegmentation/CellFeatureGenerator"
  "${CELLSEGMENTATION_BINARY_DIR}/3DCellSegmentation/CellFeatureGenerator" )
INCLUDE_DIRECTORIES( BEFORE "${CELLSEGMENTATION_SOURCE_DIR}/3DCellSegmentation/SeedExtraction"
  "${CELLSEGMENTATION_BINARY_DIR}/3DCellSegmentation/SeedExtraction" )
INCLUDE_DIRECTORIES( BEFORE "${CELLSEGMENTATION_SOURCE_DIR}/3DCellSegmentation/ROIExtract"
  "${CELLSEGMENTATION_BINARY_DIR}/3DCellSegmentation/ROIExtract" )
INCLUDE_DIRECTORIES( BEFORE "${CELLSEGMENTATION_SOURCE_DIR}/3DCellSegmentation/LevelSetBasedCellSegmentation"
  "${CELLSEGMENTATION_BINARY_DIR}/3DCellSegmentation/LevelSetBasedCellSegmentation" )
INCLUDE_DIRECTORIES( BEFORE "${CELLSEGMENTATION_SOURCE_DIR}/3DCellSegmentation/VoronoiSegmentation"
  "${CELLSEGMENTATION_BINARY_DIR}/3DCellSegmentation/VoronoiSegmentation" )
INCLUDE_DIRECTORIES( BEFORE "${CELLSEGMENTATION_SOURCE_DIR}/3DCellSegmentation/CellStatistics"
  "${CELLSEGMENTATION_BINARY_DIR}/3DCellSegmentation/CellStatistics" )
INCLUDE_DIRECTORIES(
  ${CELLSEGMENTATION_SOURCE_DIR}/Options )

SUBDIRS( ${DIRECTORIES} )
